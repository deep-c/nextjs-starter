// Custom scalar type definition for build:apollo --passthroughCustomScalars
// .generated type scalars are typed as their GraphhQl types which need to
// to defined in typescript. E.g DateTime

type DateTime = string;
